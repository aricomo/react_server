let renderToString;_b95‍.w("react-dom/server",[["renderToString",["renderToString"],function(v){renderToString=v}]]);//this module is meant to work only from node. For browserify or webpack you should use reactify or the jsx-loader.
//require('node-jsx').install({extension: '.jsx'});
//import express from 'express'
var express = require('express');
//import React from 'react'
var React = require('react');
//import ReactDOMServer from './node_modeles/react-dom/server'
var ReactDOMServer = require('react-dom/server');

//import Counter from './components/Counter.jsx'
var ReactDOM = require('react-dom');

var Counter = require('./components/Counter.jsx');
// init express
//const app = express()
var app = express();
// add top page routing
app.get('/', (req, res) => {
  

  res.sendFile(path.resolve("./public/test.html"))
})

// start listen
app.listen(3000, () => {
  console.log('Example app listening on port 3000!');
})